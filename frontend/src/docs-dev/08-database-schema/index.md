---
title: "Database"
---

Deep technical reference for Morphy's persistence layer: the primary SQLite database (`memory.db`, managed by `worker/db.ts`) that stores conversations, messages, settings, auth sessions, push subscriptions, and trusted devices, plus the separate workspace database (`app.db`). Code references point to the exact files and functions in the repository.
